<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<artifactId>jqm-webui-war-ws</artifactId>
	<parent>
		<groupId>com.enioka.jqm</groupId>
		<artifactId>jqm-webui</artifactId>
		<version>1.2.1-SNAPSHOT</version>
	</parent>
	<packaging>war</packaging>

	<name>${project.groupId}:${project.artifactId}</name>
	<url>http://jqm.readthedocs.org</url>

	<properties>
	</properties>

	<dependencies>
		<dependency>
			<groupId>com.enioka.jqm</groupId>
			<artifactId>jqm-webui-war</artifactId>
			<version>${project.version}</version>
			<type>war</type>
			<exclusions>
				<exclusion>
					<artifactId>jqm-api-client-hibernate</artifactId>
					<groupId>com.enioka.jqm</groupId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>com.enioka.jqm</groupId>
			<artifactId>jqm-api-client-jersey</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.glassfish.embedded</groupId>
				<artifactId>maven-embedded-glassfish-plugin</artifactId>
				<version>3.1.2.2</version>

				<configuration>
					<name>jqmadminwar</name>
					<contextRoot>/</contextRoot>
					<ports>
						<http-listener-1>8079</http-listener-1>
					</ports>
					<configFile>../jqm-webui-ear/src/main/resources/domain.xml</configFile>
					<systemProperties>
						<property>com.enioka.ws.url=http://tb3:51350/api/ws</property>
					</systemProperties>
				</configuration>

				<dependencies>
					<dependency>
						<groupId>org.hsqldb</groupId>
						<artifactId>hsqldb</artifactId>
						<version>${hsqldb.version}</version>
					</dependency>
					<dependency>
						<groupId>mysql</groupId>
						<artifactId>mysql-connector-java</artifactId>
						<version>5.1.29</version>
					</dependency>

					<!-- Weld update from the one provided by Glassfish - avoids HHH-8778 -->
					<dependency>
						<groupId>org.jboss.weld</groupId>
						<artifactId>weld-osgi-bundle</artifactId>
						<version>1.1.18.Final</version>
					</dependency>
				</dependencies>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<version>2.4</version>
				<configuration>
					<packagingExcludes>WEB-INF/lib/jqm-api-client-hibernate*.jar</packagingExcludes>
					<archive>
						<manifest>
							<addClasspath>true</addClasspath>
						</manifest>
						<manifestEntries>
							<Class-Path>jqm.properties</Class-Path>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>

		</plugins>
	</build>

</project>
